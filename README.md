# Library Demos

These are module demos, These come useful during development of one of the packages. You can replace
the import with a local one for development.

These packages aren't shipped with the compiler, they are downloaded as requested by the user from `chemical.mod` or `build.lab`

More things coming soon.