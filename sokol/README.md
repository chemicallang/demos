# sokol_demo

This module imports all the sokol bindings and basically tests them

### Note

It uses relative paths to modules assuming they exist relative to it, so development is done locally

If you'd like it to download the real modules from github, Use imports like

```
import "github.com/chemicallang/sokol" subdir "glue"
```
